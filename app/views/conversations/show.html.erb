<%= turbo_stream_from @conversation %>

<div class="container-fluid pt-2"
     style="position: fixed; top: 70px; left: 0; right: 0; bottom: 0;">
  <div class="row h-100 justify-content-center">
    <div class="col-10 d-flex flex-column" style="height: 100%;">
      <div class="card shadow-sm rounded-4 d-flex flex-column" style="height: 100%;">
        <div class="card-header bg-white py-3 d-flex align-items-center gap-2 border-bottom">
          <% other_user = @conversation.users.where.not(id: current_user.id).first %>
          <i class="bi bi-person-circle fs-3"></i>
          <h5 class="m-0"><%= other_user.email.split('@').first %></h5>
          <%= link_to "Back", conversations_path, class: "btn btn-sm btn-light ms-auto" %>
        </div>

        <div id="messages"
             data-controller="chat-scroll"
             data-chat-scroll-current-user-id-value="<%= current_user.id %>"
             class="p-3 overflow-auto"
             style="background: #f5f7fa; flex: 1 1 auto; min-height: 0;">
          <% @messages.each do |msg| %>
            <%= render "messages/message", message: msg, mine: msg.user_id == current_user.id %>
          <% end %>
        </div>

        <div class="card-footer bg-white p-3 border-top">
          <%= render "messages/form", conversation: @conversation, message: @new_message %>
        </div>
      </div>
    </div>
  </div>
</div>
